{{#if model}}
  {{#each model}}
    <div class='contact' data-id='{{this._id}}'>
      <a class='' href='/contacts/{{this._id}}' data-nav>{{this.name}}</a>
    </div>
  {{/each}}
{{else}}
  No contacts
{{/if}}
