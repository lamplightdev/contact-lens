{{#if model}}
  {{#each model}}
    <div class='contact' data-id='{{this.id}}'>
      <a class='' href='/contacts/{{this.id}}' data-nav>{{this.name}}</a>
    </div>
  {{/each}}
{{else}}
  No contacts
{{/if}}
